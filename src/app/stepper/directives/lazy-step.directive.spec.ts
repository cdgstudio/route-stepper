import { LazyStepDirective } from './lazy-step.directive';

describe('LazyStepDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyStepDirective();
    expect(directive).toBeTruthy();
  });
});
